import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — Firebase requires real credentials to run full integration tests.
    expect(true, isTrue);
  });
}
